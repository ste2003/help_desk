var express = require('express');
var bodyParser = require('body-parser');
var contactoDAO = require('dao/contactoDAO');
var express = require('express');

var router = express.Router();
router.use(bodyParser.json());

router.route('/')
	.get(function (req, res, next) {
		contactoDAO.buscarTodos(function(listaContacto){			
 			res.setHeader('Content-Type', 'application/json');
      		res.write(JSON.stringify(listaContacto));
      		res.end();
      	});
	})
	.post(function (req, res, next) {

		console.log('en contacto.js');
		console.log(req.body);	
		contactoDAO.insertar(
			req.body,
				function(data){
				res.setHeader('Content-Type', 'application/json');
				res.write(JSON.stringify(data));
				res.end();
				}
		);	

})
	.put(function (req, res, next) {
		contactoDAO.actualizar(
			req.body,
			function(data){
				res.setHeader('Content-Type', 'application/json');
      			res.write(JSON.stringify(data));
      			res.end();
			}
		);
	});

// Setup the item routes
router.route('/:id')
	.get(function (req, res, next) {
	var id = req.params['id'];
	if (id){
		contactoDAO.buscar(
			id,
			function(data){
				
				res.setHeader('Content-Type', 'application/json');
      			res.write(JSON.stringify(data));
      			res.end();
			}
		);
	}
	else {
		res.send(404, { status: 'Not found' });
	}
}).delete(function (req, res, next) {

		var id = req.params['id'];		
		console.log(id);
		
		contactoDAO.borrar(
			id,
			function(id){
				res.send(204);
			}
		);
	})	
.all(function (req, res, next) {  /// para todos los demás no implementado
	res.send(501, { status: 'Not implemented' });
});

module.exports = router;